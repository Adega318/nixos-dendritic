# Host Configurations

This directory contains configurations specific to individual machines (hosts).

## Hosts

| Host | Type | Status | NixOS modules | Home modules |
|------|------|--------|---------------|--------------|
| `dean/` | Laptop | Active | core, desktop, kde, bluetooth, plymouth, podman, tailscale | bottles, discord, gtk, librewolf, plasma-manager, udiskie, dev, lutris, obsidian, onlyoffice |
| `sam/` | Desktop | Active | core, desktop, kde, bluetooth, docker, plymouth, podman, tailscale, ollama, wacom | bottles, discord, gtk, librewolf, opencode, plasma-manager, udiskie, zen, dev, ff14, heroic, lutris, minecraft, calibre, obsidian, onlyoffice, retroarch, veloren |
| `castiel/` | Server | *Planned — not yet configured* | — | — |

## Directory Structure

Each host directory typically contains:

- `configuration.nix`: Main NixOS system configuration — defines `configurations.nixos.<name>` with wallpaper, theme, and the `module` listing NixOS imports.
- `home.nix`: Home Manager configuration — defines `configurations.homeManager.<name>` with the `module` listing Home Manager imports.
- `hardware-configuration.nix`: Hardware-specific settings (usually generated by `nixos-generate-config`).

## Variable Management

`variables.nix` houses global values shared across all hosts:

| Variable | Value |
|----------|-------|
| `user.username` | `adega` |
| `user.email` | `eadegafernandez@gmail.com` |
| `user.hashedPassword` | *set per-host* |
| `rootHashedPassword` | *set per-host* |

## Adding a New Host

1. Create a new directory here (e.g., `mkdir modules/hosts/<name>`)
2. Add `configuration.nix`, `home.nix`, and `hardware-configuration.nix` following the pattern of existing hosts
3. The dendritic architecture automatically discovers the new host configuration — no registration needed
